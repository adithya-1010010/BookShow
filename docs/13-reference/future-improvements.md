# Future Improvements (Out of Current Scope)

These are explicitly NOT part of the current confirmed scope and must not be implemented unless the user re-confirms them:
- Admin UI for managing movies/theatres/shows
- User authentication/accounts
- Payment integration
- VIP/seat-category pricing tiers
- Ticket export (PDF/print)
- Multi-user/networked operation
- A `Staff extends Person` class (natural extension point given the current Person/Customer hierarchy, but not required now)
