# Ticket Module

**Purpose:** Calculate ticket cost.
**Key class:** `PricingStrategy` (interface) + `StandardPricing` / `WeekendPricing`
**Consumer:** Used internally by `BookingService` — not its own screen.
**Related requirements:** FR-009
**Related phase:** Phase 5
