# Technology Stack (Confirmed — Do Not Change Without User Approval)

| Category | Choice | Notes |
|---|---|---|
| Language | Java 17 | LTS |
| GUI | JavaFX | via `org.openjfx` Maven plugin/dependencies |
| Build tool | Maven | `pom.xml` at repo root |
| Database | SQLite | file-based, local |
| DB driver | `org.xerial:sqlite-jdbc` | plain JDBC, no ORM |
| Testing | JUnit 5 (`org.junit.jupiter`) | unit + integration |
| VCS | Git / GitHub | `https://github.com/adithya-1010010/BookShow.git` |

No additional frameworks (Spring, Hibernate/JPA, Lombok, etc.) are approved. If OpenCode believes one is needed, it must stop and ask rather than adding it.
